name: knowbeforeapp
description: A final project for register points of the world that people wanna be go
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
#  cloud_firestore: ^0.13.5
  onesignal_flutter: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo-login.png
